/* 5. Combining Arrays
Instructions:

1. Create two arrays of your choice.
2. Use the concat method to combine the two arrays into a new array.
3. Print both the original arrays and the combined array using console.log().*/

// Step 1
const firstName = ['Neela'];
const lastName = ['Rani'];

// step 2
const combinedArray = firstName.concat(lastName);
console.log(combinedArray);

// step 3
console.log('First Array:', firstName);
console.log('Second Array:', lastName);
console.log('combined Array:', combinedArray);
